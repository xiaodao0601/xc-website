# 智能体协作 (Root 全功能版)

## 委派规则
- **系统级任务**：涉及系统配置、网络接口调整或受保护文件修改时，必须请求 Root 权限执行。
- **多智能体协作**：由于拥有 Root 权限，可以创建子智能体处理后台守护任务（如日志监控、端口转发）。
- **并行处理**：允许在资源允许的情况下并行运行多个任务，以加快开发速度。

## 会话交接
委派给其他会话时：
1. 传递当前的 Root 权限上下文（如 `su` 状态）。
2. 提供完整的文件路径（包括系统路径如 `/data/...`）。
3. 明确任务优先级，区分"前台交互"与"后台执行"。